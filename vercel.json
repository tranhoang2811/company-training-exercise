{
    "version": 2,
    "name": "training",
    "builds": [
       { "src": "./src/application.ts", "use": "@vercel/node" }
    ],
    "routes": [
       { "src": "/(.*)", "dest": "/application.ts" }
    ]
 }